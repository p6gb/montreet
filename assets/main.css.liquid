@font-face {
  font-family: "Chaney";
  src: url('{{ "chaney-wide-webfont.woff2" | asset_url }}') format("woff2"), url('{{ "chaney-wide-webfont.woff" | asset_url }}') format("woff"); }

*,
::after,
::before {
  box-sizing: border-box; }

/*! normalize.css v8.0.1 | MIT License | github.com/necolas/normalize.css */
/* Document
   ========================================================================== */
/**
 * 1. Correct the line height in all browsers.
 * 2. Prevent adjustments of font size after orientation changes in iOS.
 */
html {
  line-height: 1.15;
  /* 1 */
  -webkit-text-size-adjust: 100%;
  /* 2 */ }

/* Sections
   ========================================================================== */
/**
 * Remove the margin in all browsers.
 */
body {
  margin: 0; }

/**
 * Render the `main` element consistently in IE.
 */
main {
  display: block; }

/**
 * Correct the font size and margin on `h1` elements within `section` and
 * `article` contexts in Chrome, Firefox, and Safari.
 */
h1 {
  font-size: 2em;
  margin: 0.67em 0; }

/* Grouping content
   ========================================================================== */
/**
 * 1. Add the correct box sizing in Firefox.
 * 2. Show the overflow in Edge and IE.
 */
hr {
  box-sizing: content-box;
  /* 1 */
  height: 0;
  /* 1 */
  overflow: visible;
  /* 2 */ }

/**
 * 1. Correct the inheritance and scaling of font size in all browsers.
 * 2. Correct the odd `em` font sizing in all browsers.
 */
pre {
  font-family: monospace, monospace;
  /* 1 */
  font-size: 1em;
  /* 2 */ }

/* Text-level semantics
   ========================================================================== */
/**
 * Remove the gray background on active links in IE 10.
 */
a {
  background-color: transparent; }

/**
 * 1. Remove the bottom border in Chrome 57-
 * 2. Add the correct text decoration in Chrome, Edge, IE, Opera, and Safari.
 */
abbr[title] {
  border-bottom: none;
  /* 1 */
  text-decoration: underline;
  /* 2 */
  -webkit-text-decoration: underline dotted;
  text-decoration: underline dotted;
  /* 2 */ }

/**
 * Add the correct font weight in Chrome, Edge, and Safari.
 */
b,
strong {
  font-weight: bolder; }

/**
 * 1. Correct the inheritance and scaling of font size in all browsers.
 * 2. Correct the odd `em` font sizing in all browsers.
 */
code,
kbd,
samp {
  font-family: monospace, monospace;
  /* 1 */
  font-size: 1em;
  /* 2 */ }

/**
 * Add the correct font size in all browsers.
 */
small {
  font-size: 80%; }

/**
 * Prevent `sub` and `sup` elements from affecting the line height in
 * all browsers.
 */
sub,
sup {
  font-size: 75%;
  line-height: 0;
  position: relative;
  vertical-align: baseline; }

sub {
  bottom: -0.25em; }

sup {
  top: -0.5em; }

/* Embedded content
   ========================================================================== */
/**
 * Remove the border on images inside links in IE 10.
 */
img {
  border-style: none; }

/* Forms
   ========================================================================== */
/**
 * 1. Change the font styles in all browsers.
 * 2. Remove the margin in Firefox and Safari.
 */
button,
input,
optgroup,
select,
textarea {
  font-family: inherit;
  /* 1 */
  font-size: 100%;
  /* 1 */
  line-height: 1.15;
  /* 1 */
  margin: 0;
  /* 2 */ }

/**
 * Show the overflow in IE.
 * 1. Show the overflow in Edge.
 */
button,
input {
  /* 1 */
  overflow: visible; }

/**
 * Remove the inheritance of text transform in Edge, Firefox, and IE.
 * 1. Remove the inheritance of text transform in Firefox.
 */
button,
select {
  /* 1 */
  text-transform: none; }

/**
 * Correct the inability to style clickable types in iOS and Safari.
 */
button,
[type="button"],
[type="reset"],
[type="submit"] {
  -webkit-appearance: button; }

/**
 * Remove the inner border and padding in Firefox.
 */
button::-moz-focus-inner,
[type="button"]::-moz-focus-inner,
[type="reset"]::-moz-focus-inner,
[type="submit"]::-moz-focus-inner {
  border-style: none;
  padding: 0; }

/**
 * Restore the focus styles unset by the previous rule.
 */
button:-moz-focusring,
[type="button"]:-moz-focusring,
[type="reset"]:-moz-focusring,
[type="submit"]:-moz-focusring {
  outline: 1px dotted ButtonText; }

/**
 * Correct the padding in Firefox.
 */
fieldset {
  padding: 0.35em 0.75em 0.625em; }

/**
 * 1. Correct the text wrapping in Edge and IE.
 * 2. Correct the color inheritance from `fieldset` elements in IE.
 * 3. Remove the padding so developers are not caught out when they zero out
 *    `fieldset` elements in all browsers.
 */
legend {
  box-sizing: border-box;
  /* 1 */
  color: inherit;
  /* 2 */
  display: table;
  /* 1 */
  max-width: 100%;
  /* 1 */
  padding: 0;
  /* 3 */
  white-space: normal;
  /* 1 */ }

/**
 * Add the correct vertical alignment in Chrome, Firefox, and Opera.
 */
progress {
  vertical-align: baseline; }

/**
 * Remove the default vertical scrollbar in IE 10+.
 */
textarea {
  overflow: auto; }

/**
 * 1. Add the correct box sizing in IE 10.
 * 2. Remove the padding in IE 10.
 */
[type="checkbox"],
[type="radio"] {
  box-sizing: border-box;
  /* 1 */
  padding: 0;
  /* 2 */ }

/**
 * Correct the cursor style of increment and decrement buttons in Chrome.
 */
[type="number"]::-webkit-inner-spin-button,
[type="number"]::-webkit-outer-spin-button {
  height: auto; }

/**
 * 1. Correct the odd appearance in Chrome and Safari.
 * 2. Correct the outline style in Safari.
 */
[type="search"] {
  -webkit-appearance: textfield;
  /* 1 */
  outline-offset: -2px;
  /* 2 */ }

/**
 * Remove the inner padding in Chrome and Safari on macOS.
 */
[type="search"]::-webkit-search-decoration {
  -webkit-appearance: none; }

/**
 * 1. Correct the inability to style clickable types in iOS and Safari.
 * 2. Change font properties to `inherit` in Safari.
 */
::-webkit-file-upload-button {
  -webkit-appearance: button;
  /* 1 */
  font: inherit;
  /* 2 */ }

/* Interactive
   ========================================================================== */
/*
 * Add the correct display in Edge, IE 10+, and Firefox.
 */
details {
  display: block; }

/*
 * Add the correct display in all browsers.
 */
summary {
  display: list-item; }

/* Misc
   ========================================================================== */
/**
 * Add the correct display in IE 10+.
 */
template {
  display: none; }

/**
 * Add the correct display in IE 10.
 */
[hidden] {
  display: none; }

body,
h1,
h2,
h3,
h4,
h5,
h6,
p,
blockquote,
pre,
dl,
dd,
ol,
ul,
form,
fieldset,
legend,
figure,
table,
th,
td,
caption,
hr {
  margin: 0;
  padding: 0; }

.hr {
  margin-right: auto;
  margin-left: auto;
  width: 50%;
  margin-bottom: 2em;
  background-color: #0c0d49;
  height: 2px;
  border: none; }
  @media screen and (min-width: 576px) {
    .hr {
      width: 7em;
      margin-bottom: 2rem; } }
  .hr--white {
    background-color: #ffffff; }
  .hr--margin-top {
    margin-top: 2em; }
    @media screen and (min-width: 576px) {
      .hr--margin-top {
        margin-top: 3em; } }
  .hr--margin-bottom-big {
    margin-bottom: 2em; }
    @media screen and (min-width: 576px) {
      .hr--margin-bottom-big {
        margin-bottom: 3em; } }

.h3 {
  font-family: var(--typeChaneyPrimary), var(--typeChaneyFallback);
  font-size: var(--typeChaneySize);
  font-weight: var(--typeChaneyWeight);
  line-height: var(--typeChaneyLineHeight);
  letter-spacing: var(--typeChaneySpacing);
  -webkit-hyphens: auto;
  -ms-hyphens: auto;
  hyphens: auto; }

.h4 {
  margin-bottom: 0.5em; }

.grid-product__meta {
  padding: 1rem 0 0.5em 0; }

.grid-product__content {
  text-align: center; }

.grid-product__colors {
  justify-content: center; }

/* with the original setting of height: 160% and top: -30%
 the image is zoomed too much */
.parallax-container {
  top: 0;
  height: 115%; }

.section-header {
  margin-bottom: 2em; }
  .section-header__title {
    font-family: var(--typeChaneyPrimary), var(--typeChaneyFallback);
    font-size: var(--typeChaneySize);
    font-weight: var(--typeChaneyWeight);
    line-height: var(--typeChaneyLineHeight);
    letter-spacing: var(--typeChaneySpacing); }
    .section-header__title--left-aligned {
      text-align: left;
      margin-left: 50px;
      text-indent: -10px; }
      .section-header__title--left-aligned:before {
        background-color: #0c0d49;
        content: "";
        display: inline-block;
        height: 3px;
        position: relative;
        width: 25px;
        right: 0.5em;
        top: -0.3em;
        margin-left: -25px; }
        @media screen and (min-width: 768px) {
          .section-header__title--left-aligned:before {
            width: 50px;
            margin-left: -50px; } }
      @media screen and (min-width: 768px) {
        .section-header__title--left-aligned {
          padding-left: 25px;
          text-indent: -35px; } }
    .section-header__title--centered {
      text-align: center; }
  .section-header--hero-overlay {
    position: absolute;
    top: 50%;
    color: white;
    transform: translateY(-50%); }

.newsletter-section {
  color: #0c0d49;
  background-color: #fdf7e7; }
  .newsletter-section__title {
    margin-bottom: 2rem; }
  .newsletter-section__text {
    margin-bottom: 1rem; }

.text-box {
  padding-top: 2em;
  padding-bottom: 2em; }
  @media screen and (min-width: 576px) {
    .text-box {
      padding-top: 3em;
      padding-bottom: 5em; } }

.btn--red {
  background-color: #a61315; }

@media screen and (max-width: 767px) {
  .feature-row__image {
    margin-bottom: 1em; }
  .feature-row__text {
    text-align: center; } }

.footer {
  display: flex; }
  @media screen and (max-width: 767px) {
    .footer {
      flex-direction: column; }
      .footer__collapsible p {
        text-align: center;
        margin-bottom: 1em; }
      .footer__title {
        text-align: center; }
      .footer__newsletter-input {
        max-width: none; }
      .grid__item .footer__social {
        text-align: center;
        margin-bottom: 0.5em; }
        .grid__item .footer__social li {
          margin-right: 10px;
          margin-left: 10px; } }
  @media screen and (min-width: 768px) {
    .footer {
      flex-flow: row nowrap;
      align-items: flex-start; }
      .footer__item--border-left {
        border-left: 1px solid #cacaca;
        margin-left: 15px;
        padding-left: 25px; } }
  .footer__social {
    margin-bottom: 1em; }
  @media screen and (max-width: 767px) {
    .footer__logo {
      width: 100%; }
      .footer__logo img {
        margin-right: auto;
        margin-left: auto; } }
  .footer__collapsible p {
    margin-bottom: 1.5em; }
  .footer__newsletter input {
    background-color: #ffffff; }

@media screen and (max-width: 767px) {
  .site-footer__linklist {
    text-align: center; }
  .site-footer .grid__item::after {
    content: none;
    border-bottom: none; } }

.color-swatch,
.color-swatch--small {
  width: 15px;
  height: 15px;
  border-radius: 50%; }
  @media screen and (min-width: 576px) {
    .color-swatch,
    .color-swatch--small {
      width: 25px;
      height: 25px; } }
  .color-swatch::before,
  .color-swatch--small::before {
    display: none;
    border-radius: 50%; }

label.color-swatch,
label.color-swatch--small {
  padding: 0; }

.rent-button {
  margin-top: 10px;
  background: #b25ba5 !important; }

.article__featured-image {
  position: relative; }

.article__title {
  margin-top: 0; }
  .article__title:hover {
    text-decoration: underline; }

@media screen and (max-width: 589px) {
  .article__image, .article__image-wrap {
    vertical-align: middle;
    display: inline-block; } }

#shopify-section-page-press-template a.article__grid-image {
  border-bottom: 0; }

.press-article__text-container {
  text-align: center; }

.team {
  display: flex;
  flex-flow: row wrap; }
  @media screen and (max-width: 575px) {
    .team__container {
      width: 100%;
      padding-left: 0; } }
  .team__image-wrap {
    margin-right: auto;
    margin-left: auto; }
  .team__image {
    border-radius: 50%;
    padding: 2em; }
  .team__name, .team__role {
    display: inline-block;
    margin-bottom: 0.7em; }
  .team__name {
    font-weight: 700; }
  .team__role {
    text-transform: uppercase;
    opacity: 0.7; }
  .team__motto {
    font-style: italic;
    margin-bottom: 1em; }
  .team__description p {
    margin-bottom: 1.5em; }

.social-links a {
  text-decoration: none;
  color: inherit;
  border-bottom: none !important; }
  .social-links a::after {
    display: none; }

.social-links li {
  margin-right: 0.3em;
  margin-left: 0.3em;
  display: inline-block; }

.title-image__text-container-outer {
  display: flex;
  z-index: 1;
  width: 100%;
  height: 100%;
  justify-content: center; }
  .title-image__text-container-outer .animation-cropper {
    align-self: center; }
  @media screen and (min-width: 590px) {
    .title-image__text-container-outer {
      position: absolute; } }

.title-image__text-container-inner {
  text-align: center;
  padding: 1em;
  width: auto;
  background-color: none;
  color: #0c0d49; }
  @media screen and (min-width: 590px) {
    .title-image__text-container-inner {
      color: #ffffff; } }
  @media screen and (min-width: 768px) {
    .title-image__text-container-inner {
      padding: 2.5em; } }

.title-image__title {
  font-family: var(--typeChaneyPrimary), var(--typeChaneyFallback);
  font-size: var(--typeChaneySize);
  font-weight: var(--typeChaneyWeight);
  line-height: var(--typeChaneyLineHeight);
  letter-spacing: var(--typeChaneySpacing);
  word-wrap: break-word; }
  @media screen and (min-width: 590px) {
    .title-image__title {
      margin-bottom: 1em; } }

@media screen and (max-width: 589px) {
  .title-image__title {
    color: #0c0d49;
    font-size: 1.7rem;
    margin-top: 0.5em; }
  .title-image .background-media-text {
    background: none; }
  .title-image .hr {
    display: none; } }

.sdgs__item {
  padding-right: 1em;
  padding-left: 1em;
  display: flex;
  justify-content: flex-start;
  flex-flow: column nowrap;
  width: 100%;
  cursor: pointer; }
  @media screen and (min-width: 576px) {
    .sdgs__item {
      width: 50%; } }
  @media screen and (min-width: 768px) {
    .sdgs__item {
      width: 25%; } }

.sdgs__image {
  cursor: cell; }

.sdgs__container {
  margin-right: -1em;
  margin-left: -1em;
  display: flex;
  justify-content: flex-start;
  flex-flow: row wrap; }

.sdgs__text-container {
  padding-right: 2em;
  padding-left: 2em;
  display: none;
  width: 100%;
  height: auto;
  margin-bottom: 2.5em; }
  @media screen and (min-width: 576px) {
    .sdgs__text-container {
      padding-right: 12.5%;
      padding-left: 12.5%; } }

.sdgs__text-container .sdgs__title {
  margin-bottom: 0.5em; }

.sdgs__image-container {
  margin-bottom: 2em; }

.sdgs__intro-text {
  position: relative;
  text-align: center;
  margin-bottom: 3em; }

.partner__category-container {
  margin-bottom: 3em; }

.partner__intro {
  position: relative;
  margin-bottom: 2em; }
  @media screen and (min-width: 768px) {
    .partner__intro {
      margin-bottom: 4em; } }

.partner__section-title {
  text-align: center; }

#shopify-section-page-partner-template {
  margin-top: 0; }

.tooltip {
  position: relative; }

/* Tooltip text */
.tooltip .tooltiptext {
  visibility: hidden;
  width: auto;
  background-color: black;
  color: #fff;
  text-align: center;
  padding: 5px 10px;
  border-radius: 6px;
  /* Position the tooltip text - see examples below! */
  position: absolute;
  z-index: 1;
  top: 100%;
  left: 50%;
  margin-left: -50%; }

/* Show the tooltip text when you mouse over the tooltip container */
.tooltip:hover .tooltiptext {
  visibility: visible; }

.tooltiptext {
  transition: opacity 200ms linear; }

.cart-additional-info {
  background-color: #b25ba5;
  text-align: center;
  color: #ffffff;
  padding: 2em; }
  .cart-additional-info__title {
    font-size: 1em;
    padding-bottom: 0.5em;
    text-transform: uppercase; }
  .cart-additional-info__text a {
    color: #ffffff;
    text-decoration: underline; }

.product-contact-form-link__link {
  text-decoration: underline; }

ul.tabs {
  border-bottom: 1px solid #dddddd;
  display: block;
  margin: 0 0 20px;
  padding: 0; }

ul.tabs li.tab-content {
  display: block;
  margin-bottom: 0;
  padding: 0;
  width: auto;
  height: 2.5em; }

ul.tabs li a {
  display: block;
  margin: 0;
  padding: 0 20px 0 0;
  text-decoration: none;
  width: auto; }

ul.tabs li a.active {
  background: none repeat scroll 0 0 #ffffff;
  border-left-width: 1px;
  border-top-left-radius: 2px;
  border-top-right-radius: 2px;
  color: #0c0d49;
  position: relative; }

/* update for better mobile look */
ul.tabs.vertical > li {
  width: 100%; }

.tab-content {
  margin-bottom: 1em;
  margin-top: 1em; }

.tab-li {
  list-style: none; }

.tab-link {
  height: 3em;
  line-height: 3em;
  font-weight: 400; }
  .tab-link.active {
    font-weight: 700; }

@media screen and (max-width: 589px) {
  .hero__text-content h2.hero__title {
    font-size: 2rem; } }

@media screen and (max-width: 589px) {
  [data-dots="true"] .hero__text-content {
    padding-right: 0; } }

.lightboxlink {
  margin-bottom: 2em;
  display: block;
  padding-bottom: 10px; }

@media screen and (min-width: 590px) {
  .shopify-email-marketing-confirmation__container {
    padding-top: 25vh; } }

.shopify-email-marketing-confirmation__container h1 {
  font-family: var(--typeChaneyPrimary), var(--typeChaneyFallback);
  font-size: var(--typeChaneySize);
  font-weight: var(--typeChaneyWeight);
  line-height: var(--typeChaneyLineHeight);
  letter-spacing: var(--typeChaneySpacing);
  margin-bottom: 1rem; }

.shopify-email-marketing-confirmation__container a {
  margin-top: 1.5rem;
  font-size: 0.75em;
  text-decoration: underline;
  display: block; }

.product__donation-notice {
  padding: 1em 3em;
  text-align: center;
  margin-bottom: 1.5em; }

.product__icon-container {
  background-color: #e8e8f1;
  padding: 2em 1.5em 1.5em;
  margin-bottom: 2.5em; }

.product__icon {
  width: 1.8em;
  display: inline-block;
  vertical-align: middle;
  margin-right: 1ch; }

.product__icon-message {
  display: inline-block; }

.product__icon-row {
  display: flex;
  flex-flow: row nowrap;
  align-items: center;
  margin-bottom: 0.6em; }

.popup {
  display: block;
  flex-flow: row nowrap; }
  .popup__text-container {
    text-align: left; }
    @media (min-width: 768px) {
      .popup__text-container {
        padding-left: 30px;
        margin-left: 50%; } }
  .popup__image-container {
    position: absolute;
    top: 0;
    right: 50%;
    bottom: 0; }
    @media (max-width: 768px) {
      .popup__image-container {
        display: none; } }
    .popup__image-container img {
      width: 100%;
      height: 100%;
      -o-object-fit: cover;
      object-fit: cover; }

.newsletter {
  max-width: 680px; }

.modal .newsletter .h1 {
  margin-bottom: 20px; }

.modal .newsletter p {
  margin-bottom: 10px; }

.modal .popup__disclaimer p {
  font-size: 0.85em;
  margin-bottom: 1em; }
  @media (min-width: 768px) {
    .modal .popup__disclaimer p {
      margin-top: 2.5em;
      margin-bottom: 0; } }

.modal .newsletter .popup__image-container {
  margin-bottom: 0; }

.newsletter__form {
  margin-top: 6px; }
  @media (min-width: 768px) {
    .newsletter__form {
      margin-top: 20px; } }

.newsletter__input {
  border-color: #0c0d49;
  padding: 11px;
  text-align: center; }
  .newsletter__input::-moz-placeholder {
    color: #cacaca; }
  .newsletter__input:-ms-input-placeholder {
    color: #cacaca; }
  .newsletter__input::placeholder {
    color: #cacaca; }

@media (min-width: 768px) {
  .newsletter__input-group {
    flex-direction: column; }
    .newsletter__input-group .input-group-btn {
      margin-top: 10px; } }

.newsletter__input-group .btn {
  width: 100%; }

.faq__question {
  font-size: 1.2em;
  margin-bottom: 0.5em; }

.rte img {
  height: auto; }

.rte--centered {
  text-align: center;
  margin-left: auto;
  margin-right: auto; }

/*
[data-type_header_accent_transform="true"] .label,
[data-type_header_accent_transform="true"] label,
button.collapsible-trigger-btn {
  text-transform: none;
  font-size: 1rem;
}
*/
